import Mock from 'mockjs'
import { CmdData } from '@/utils/request/axios'

export const useMock = () => {
  // const mock = (url: string, method: 'post' | 'get' | 'delete' | 'put' | 'patch', data: CmdData) => {
  //   Mock.mock('/card/list','get', data)
  // }
}